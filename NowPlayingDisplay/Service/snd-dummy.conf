# Auto-load snd-dummy at boot for NowPlayingDisplay's visualizer SqueezeLite
# instance. snd-dummy is a virtual sound card kernel module already shipped
# in stock kernels for every major distro — no compilation needed.
snd-dummy
